
# Getting Started

## Quick-launch with Docker
```sh
docker compose up
```


## Local Development
```sh
# Server
cd ./server && yarn && yarn dev

# Client
cd ./client && yarn && yarn dev

# Postgres
docker compose up db
```